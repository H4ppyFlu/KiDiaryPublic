# Alembic's own settings. The database URL is not here: it comes from the
# application's configuration (see `migrations/env.py`), so there is one source of
# truth for where the database is.

[alembic]
script_location = migrations
prepend_sys_path = .
path_separator = os
